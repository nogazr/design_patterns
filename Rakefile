task default: %w[test]

task :composition do
  ruby 'app/composition.rb'
end

task :decorator do
  ruby 'app/decorator.rb'
end

task :report do
  ruby 'app/report.rb'
end

task :strategy do
  ruby 'app/strategy.rb'
end
